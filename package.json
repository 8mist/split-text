{
  "name": "split-text",
  "description": "SplitText is a small and type-safe library that split HTML text into characters and/or words.",
  "version": "0.3.1",
  "license": "gpl-3.0",
  "engines": {
    "node": ">=18.0.0",
    "yarn": ">=1.22.0",
    "pnpm": ">=8.0.0"
  },
  "author": {
    "name": "Grégoire Ciles",
    "url": "https://twitter.com/gregoireciles"
  },
  "scripts": {
    "build": "turbo run build",
    "build:core": "turbo run build --filter=@gregoire.ciles/split-text",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "preview": "turbo run preview",
    "check-size": "turbo run check-size",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "release": "changeset version",
    "pub:release": "turbo run pub:release"
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^3.7.2",
    "@types/node": "^20.3.3",
    "prettier": "^2.8.8",
    "turbo": "^1.10.7",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@changesets/changelog-github": "^0.4.8",
    "@changesets/cli": "^2.26.2"
  }
}
